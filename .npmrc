# NPM configuration for @weaveaijs scope
registry=https://registry.npmjs.org/
@weaveaijs:registry=https://registry.npmjs.org/

# Enable publishing with provenance attestation (optional)
# provenance=true

# Ensure strict peer dependency checking
strict-peer-dependencies=false
