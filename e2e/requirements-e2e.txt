# Dependencies for the live platform E2E suite (e2e/). Not part of the
# mcp-observatory package -- setuptools only picks up mcp_observatory*, so
# nothing here reaches the published wheel.
boto3>=1.34
botocore>=1.34
requests>=2.31
pytest>=8.0
